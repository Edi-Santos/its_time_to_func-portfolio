function studentStats(grade) {
  // Substitua o return pelo seu código
  return undefined;
}

function calculator(sign, n1, n2) {
  // Substitua o return pelo seu código
  return undefined;
}

function conversionTempMeasur(degrees, currentMeasure, conversion) {
  // Substitua o return pelo seu código
  return undefined;
}

module.exports = {
  studentStats,
  calculator,
  conversionTempMeasur,
}
